@inherits PresentationComponentBase

<button type="@_htmlButtonType" class="@(_class)" onclick="@(() => OnClickHandler())" disabled="@Disabled">
    
    @if (!string.IsNullOrEmpty(Icon))
    {
        <i class="@(_iconClass)" />
    
    }

    <p class="@(_labelClass)">@Label</p>

</button>